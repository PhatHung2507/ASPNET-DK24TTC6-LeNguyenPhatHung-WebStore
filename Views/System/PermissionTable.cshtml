@model WebStore.Models.PageViewModel<WebStore.Models.Entities.Permission>
@using System.Linq

<div class="table-container">
    <table class="data-table">
        <colgroup>
            <col>
        </colgroup>
        <thead>
            <tr>
                <th>Tên</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Items)
            {
                <tr class="data-row" data-id="@item.Id">
                    <td class="text-center">@item.Name</td>
                </tr>
            }
        </tbody>
    </table>
</div>
@{
    Model.BaseUrl = "/system/menucategoryweblist/partialTableMenucategoryWeb";
    Model.ReloadFunctionName = "reloadAdminTable";
}
@await Html.PartialAsync("PaginationPartial", Model)
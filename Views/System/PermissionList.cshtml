@using WebStore.Models.Entities
@model WebStore.Models.PageViewModel<WebStore.Models.Entities.Permission>

@{
    ViewData["Title"] = "Nhóm quyền";
}
<form method="get">
    <div class="search-form">
        <div class="item">
            <div class="name">Tên nhóm</div>
            <div class="val">
                <input type="text" class="form-control TextBox" name="name" value="@ViewData["SearchName"]" />
            </div>
        </div>
        <div class="item">
            <div class="name"></div>
            <div class="val">
                <button class="Button" type="submit">
                    <i class="fa fa-search icon"></i>Lọc dữ liệu
                </button>
                <a href="/system/permissionlist" class="Button">
                    <i class="fa fa-refresh icon"></i>
                </a>
            </div>
        </div>
    </div>
</form>
<div class="clear10"></div>
@await Component.InvokeAsync("PermissionButtons", new { menuId = "PHANQUYEN", modal = "modalPermission" })
<div class="clear10"></div>
<div id="dataTableContainer">
    @await Html.PartialAsync("~/Views/System/PermissionTable.cshtml", Model)
</div>
@await Html.PartialAsync("ShowFormPartial", Model, new ViewDataDictionary(ViewData) { { "ModalId", "modalPermission" } })
@section Scripts {
    <script>
        $(function() {
            setupDeleteButton(".btn-delete",".data-row",function($row) {return $row.data("id");},"/system/permissionlist/delete/","/system/permissionlist/partialTablePermission");
        });
    </script>
}